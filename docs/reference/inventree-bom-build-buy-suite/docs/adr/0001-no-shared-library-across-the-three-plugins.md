# No shared library across the three plugins

We keep each plugin as its own repo and codebase. A shared `inventree-bom-toolkit` should only be extracted once a third plugin proves the need, not before.
