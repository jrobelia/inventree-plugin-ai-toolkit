# Build Order Generator lives on the Build Order detail page

Build Order Generator lives on the Build Order detail page, not the Part page. A parent Build Order already has a defined build quantity and project code, so putting it on the Part page would force a hypothetical quantity guess.
