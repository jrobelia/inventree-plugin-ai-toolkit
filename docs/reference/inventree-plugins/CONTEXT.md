# Context: InvenTree Plugins

This context covers the shared conventions, structure, and workflow for any InvenTree-based plugin developed inside the toolkit. It is independent of any specific plugin domain.

## Scope

Applies to every plugin under `plugins/`. It covers:

- The standard plugin package layout (`<plugin_name>/`, `frontend/`, `pyproject.toml`, `README.md`).
- InvenTree plugin mixins (`SettingsMixin`, `UrlsMixin`, `UserInterfaceMixin`, `EventMixin`, `ScheduleMixin`).
- The devcontainer-based development environment and common commands.
- Documentation standards and file naming conventions.
- The test pyramid: unit tests, integration tests, and Playwright E2E tests.
- Build and deployment workflow.

It does **not** cover:
- The BOM/Build/Buy suite-specific domain model (see [`inventree-bom-build-buy-suite/CONTEXT.md`](../inventree-bom-build-buy-suite/CONTEXT.md)).
- The internal implementation details of any individual plugin (see `plugins/<name>/CONTEXT.md`).

## Key concepts

- **InvenTree Plugin:** A Python package that extends InvenTree using the `plugin` API and mixins.
- **Mixin:** A capability interface provided by InvenTree (e.g., `UrlsMixin` for API endpoints, `UserInterfaceMixin` for UI panels).
- **Plugin skeleton:** The minimal package generated by the `plugin-creator` tool (run as `create-inventree-plugin` or `python -m plugin_creator.cli` in the devcontainer).
- **Plugin type:** A classification based on the mixins a plugin declares.
  - **UI plugin:** Uses `UserInterfaceMixin` to render React components (panels, dashboard items, etc.) in the InvenTree SPA. Requires a `frontend/` package and Playwright E2E tests.
  - **Headless plugin:** Uses only backend mixins (`EventMixin`, `ScheduleMixin`, `UrlsMixin`, `ValidationMixin`, `SupplierMixin`, `BarcodeMixin`, `LabelPrintingMixin`, etc.). Has no `frontend/` and no E2E tests.
  - **Hybrid plugin:** Uses `UserInterfaceMixin` plus backend mixins. Gets both frontend and backend test layers.
- **Vertical slice:** One feature implemented end-to-end (user story → test → implementation → API → UI → docs).
- **Devcontainer:** The Docker-based environment that hosts InvenTree and the plugins for development.
- **Deterministic test chain:** The sequence of unit, integration, and E2E tests run before considering a feature done. `scripts/run-test-all.sh` is the canonical toolkit-level runner; it checks for a healthy InvenTree server, starts one if needed, and then calls the plugin's `test-all.sh`. The `.devin/skills/test-inventree-plugin/SKILL.md` skill is the agent entry point.

## Related reference docs

- [`plugins/README.md`](../../plugins/README.md) — directory structure and git repo rules.
- [`DOCUMENTATION-STANDARDS.md`](./DOCUMENTATION-STANDARDS.md) — naming and structure for plugin docs.
- [`PLUGIN-DEVELOPMENT-WORKFLOW.md`](./PLUGIN-DEVELOPMENT-WORKFLOW.md) — iterative TDD workflow.
- [`SESSION-ONBOARDING.md`](./SESSION-ONBOARDING.md) — reproducible steps for each dev session.
- [`reference/README.md`](../../reference/README.md) — InvenTree core source code and example plugins used as implementation reference.

## Decisions

Generic plugin tooling and process decisions live in `docs/decisions.md` or `docs/adr/` at the toolkit root.
